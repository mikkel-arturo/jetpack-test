{
  "name": "@jetpack-test/service1",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx ttsc",
    "show": "rm -rf dish && yarn build && cd dist && node index.js"
  },
  "dependencies": {
    "aws-lambda": "^1.0.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.81",
    "@zerollup/ts-transform-paths": "^1.7.18",
    "ttypescript": "^1.5.12",
    "typescript": "^4.3.5"
  }
}
